import LatestPrice from "./latestPrice";
import { shallow } from "enzyme";

describe("Test LatestPrice page", () => {
  it("renders a table", () => {
    // const page = shallow(<LatestPrice />);
    // console.log(page);
  });
});
